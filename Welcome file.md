

# <center>GNU/Linux shell 进阶实验</center>


##  姓名：李曾洋   学号：23020007055
## 一、实验内容

### 1、文件系统相关命令：ls、cd、pwd、mkdir、touch、cp、mv、rm、cat、 find、grep、more、less、head、tail、ln、stat、file

### 2、管道符号

### 3、权限相关命令：chmod、chown、chgrp，特殊权限：suid、sgid、sbit

### 4、内存相关命令：free、top、ps、pmap、vmstat、sar

### 5、内存信息：/proc/pid/maps、/proc/pid/smaps

### 6、网络相关命令：ifconfig、netstat、curl

### 7、shell脚本

## 实验目的
### 本次实验聚焦Linux系统管理与Shell编程，涵盖文件与权限管理（ls/find/chmod）、内存分析（free/top）、网络监控（netstat/curl）及脚本开发，通过命令组合（管道|）、权限机制（SUID/SBIT）、内存映射（pmap）等实践，掌握系统调用原理与自动化运维技能，为服务部署及系统诊断打下基础。
## 实验步骤
### 1. 文件系统及管道
### ① 执行以下命令并说明其功能：`Is /home | grep "^test"`
#### `ls /home`是列出 `/home` 目录下的所有文件和子目录。`|`是管道符，将 `ls /home` 的输出作为输入传递给 `grep` 命令。 `grep "^test"`：筛选出前面传过来的以 `test` 开头的行、文件名或者目录名。
<img src="/imgs/2025-03-08/5VWlWvzd7RZVhH9B.png" 
     alt="示例图片"
     style="display:block; margin:auto; max-width:80%; height:auto;">

### ②使用管道命令将 `cat` 的输出传递给 `head` 命令，显示文件的前4行内容。
![输入图片说明](/imgs/2025-03-08/gqNRJl8aM3hBSjLq.png)

### ③ 使用管道命令将`/lib`目录下的文件按字母顺序对文件名进行排序。

~~~bash
ls /lib | sort #正序
~~~
![正序](/imgs/2025-03-08/SCbpRgwBtyum9kuc.png)

~~~bash
ls /lib | sort #逆序
~~~
![逆序](/imgs/2025-03-08/cR0Tjcn8ItBSOSmB.png)
### ④ 思考：为什么两个命令之间不能使用变量或者内存直接传递数据？
#### 基本原理：我感觉像曲老师上课讲的，世上没有完美的系统。这种做法我感觉像是为了安全性而牺牲一定的性能，如果能够直接使用变量或者内存直接传递数据这种方式虽然很快，性能更高，但是不安全，所以为了安全不得已这么做。

#### 直接原因：**每个进程拥有独立的内存空间**，  操作系统通过虚拟内存管理为每个进程分配独立的内存地址空间。进程 A 无法直接访问进程 B 的内存。

#### 目的：出于对计算机的安全性和稳定性考虑，独立的内存空间可以防止恶意程序篡改其他进程的数据，也可以避免因为一个进程的崩溃而影响到其他进程。

### 2. 权限
### ① 使用`man`学习`chown`和`chgrp`的功能及用法。
#### `chown`:用于更改文件或目录的所有者或所属组。
####  `chgrp`:用于修改文件或目录的所属用户组。
~~~bash
man chown #man chgrp，但是老师好像限制我们操作了
~~~
![输入图片说明](/imgs/2025-03-09/9T40UwFg4F3J93jO.png)

### ② 在桌面下创建一个目录`data`，使用` chmod `命令将目录`data`设置为 `STICKY BIT` 权限，从而使得在该目录下创建的文件只能被其所有者和超级用户删除。说明：该权限位什么含义？
#### 含义： 在设置了粘滞位的目录中，​只有所有者和超级用户能删除或重命名文件，而其他用户对该目录有写权限，但无法删除或重命名该文件。可以防止误删或恶意删除他人文件，我感觉在协同开发时这个对项目管理很有用。
![输入图片说明](/imgs/2025-03-08/1z9CJod0qCzhfZkd.png)
### 3. 内存
### ① 使用 `free` 命令查看系统的内存使用情况，并解释其中的列名称和含义
| ​**列名**       | ​**说明**                                                                 | ​**如图​**       |
|:-------------------:|-------------------------------------------|:-------------------------------:|
| ​**total**      | 总内存：系统的物理内存或交换空间总量                                      | 31Gi|
| ​**used**       | 已用内存：当前已被系统或进程占用的内存                                    | 2.3Gi|
| ​**free**       | 空闲内存：完全未被使用的内存（不包含缓存和缓冲区）                        | 19Gi      |
| ​**shared**     | 共享内存：被多个进程共享的内存（如共享库、IPC 通信）                       | 26Mi            |
| ​**buff/cache** | 缓冲/缓存：内核用于磁盘缓存和缓冲区的内存（可被快速回收）                  | 9.4Gi|
| ​**available**  | 可用内存：预估可分配给新进程的内存     | 28.5Gi        |

![输入图片说明](/imgs/2025-03-08/2gFX9DjNXSRKOfYk.png)

### ② 使用 `top` 命令查看系统的内存占用情况，并解释其中的内存使用率显示
####  **内存（Mem）**
| 项目 | 值（MiB） | 说明 |
|--------------|:-----------:|------|
| **总内存（total）** | **32090.4** | 系统的总物理内存 |
| **空闲内存（free）** |  **112.6** | 当前未被使用的内存 | 
| **已使用内存（used）** | **2344.8** | 进程正在使用的物理内存 |
| **缓存（buff/cache）** | **9633.0** | 被系统用于缓存文件的数据，可随时释放 |
#### **交换分区（Swap）** 
| 项目 | 值（MiB） | 说明 |
|--------------|:-----------:|------|
| **总交换空间（total）** | **8192.0** | 系统分配的交换空间（虚拟内存） |
| **空闲交换空间（free）** | **8182.6** | 当前未被使用的交换空间 |
| **已使用交换空间（used）** | **9.4** | 当前正在使用的交换空间 |

![输入图片说明](/imgs/2025-03-08/XZcJDNQa6HAdXe2d.png)
### ③ 使用 `ps` 命令查看当前系统中内存占用最高的进程，并显示其进程`ID`和内存占用。
```bash
ps -eo pid,%mem,cmd --sort=-%mem | head -n 2
```
![输入图片说明](/imgs/2025-03-08/PtAgzFkeeHH9HLK8.png)
### ④ 使用 `pmap` 命令查看指定进程的内存映射情况，并解释输出中的地址范围和映射类型。
| 地址范围                           | 大小   | 映射类型  | 描述                      |
|:---------------:|:------:|:---------:|---------------------------|
| `000055ca61a12000-000055ca61a30000` | 180KB  | `r----`   | **bash** 可执行文件的只读部分（代码段） |
| `000055ca61a3f000-000055ca61af0000` | 708KB  | `r-x--`   | **bash** 可执行文件的可执行部分（代码段） |
| `000055ca61af0000-000055ca61b27000` | 220KB  | `r----`   | **bash** 只读数据部分（常量数据） |
| `000055ca61b27000-000055ca61b2b000` | 16KB   | `r----`   | **bash** 只读数据部分 |
| `000055ca61b2b000-000055ca61b34000` | 36KB   | `rw---`   | **bash** 可读写内存（动态分配内存） |
| `00007f82aa9e5000-00007f82aa9e6000` | 12KB   | `r----`   | `libnss_files-2.31.so` 库的只读数据 |
| `00007f82aa9e8000-00007f82aa9ef000` | 28KB   | `r-x--`   | `libnss_files-2.31.so` 库的代码段 |
| `00007f82aa9ef000-00007f82aa9f1000` | 8KB    | `r----`   | `libnss_files-2.31.so` 库的只读数据 |
| `00007f82aa9f2000-00007f82aa9f3000` | 4KB    | `rw---`   | `libnss_files-2.31.so` 库的可写数据 |
| `00007ffe1f87f000-00007ffe1f8b0000` | 132KB  | `rw---`   | **bash** 进程的栈 |
| `00007ffe1f8f4000-00007ffe1f8f8000` | 16KB   | `r----`   | 匿名映射（堆或其他动态分配内存） |
| `00007ffe1f8f8000-00007ffe1f8f9000` | 8KB    | `r-x--`   | 匿名映射 |
| `ffffffffff600000`                 | 4KB    | `--x--`   | 匿名映射（特殊区域） |

![输入图片说明](/imgs/2025-03-09/z3jswM0PkcnxGNpV.png)
### ⑤ 使用 `vmstat` 命令实时监控系统的虚拟内存使用情况，并解释输出中的各列含义。
| **分类** | **字段**    | **值**        | **含义**                        |
|----------|:-------------:|:---------------:|---------------------------------|
| **进程** | `r`         | 0             | 当前无进程等待 CPU 执行         |
|          | `b`         | 0             | 无进程因 I/O 阻塞               |
| **内存** | `swpd`      | 9,644 KB      | 少量使用交换分区，不影响性能     |
|          | `free`      | 20.6 GB       | 可用物理内存非常充足            |
|          | `buff/cache`| 1.3G/8.5G     | 缓存机制正常利用内存            |
| **I/O**  | `bi/bo`     | 0             | 无磁盘读写操作                  |
| **CPU**  | `id`        | 100%          | CPU 完全空闲                    |


![输入图片说明](/imgs/2025-03-08/3PZoPWqgRro6tEqL.png)
### ① 使用 `sar` 命令查看系统的内存利用率，并解释输出中的各列含义。
#### 这个需要sudo权限，我就在自己的虚拟机里执行了
| **列名** | **描述** |
|:--------------:|-----------|
 | **kbmemfree** | 系统中空闲的内存。 | 
 | **kbavail** | 可用内存的总量（包括空闲内存和缓存的内存）。 |
  | **kbmemused** | 已使用的内存。 |
  | **%memused** | 已使用内存占总内存的百分比。 | 
 | **kbbuffers** | 用于缓冲的内存。 | 
| **kbcached** | 用于文件缓存的内存。 |
| **kbcommit** | 系统中已承诺分配的内存，即已经分配但尚未使用的内存（如预留内存）。 |
  | **%commit** | 已承诺的内存占总内存的百分比。 | 
 | **kbactive** | 当前活跃的内存，即频繁使用的内存。 | 
  | **kbinact** | 当前不活跃的内存，即不常使用的内存。 | 
| **kbdirty** | 被标记为脏页（需要写回磁盘）的内存。 |
![输入图片说明](/imgs/2025-03-09/LnNYB3sFkAZHEnc1.png)
### 4. 网络、`binutils`
### ① 使用 `netstat` 命令查看当前系统的网络连接状态，并且只显示`TCP`连接。
![输入图片说明](/imgs/2025-03-08/9Wu9oLshWE703u0f.png)

### ② 自学常用的`binutils`工具。
 1. `ld`（`as -o lzy.o lzy.s`） - 链接器，用于将目标文件（`.o` 文件）和库文件链接成一个可执行文件。
 2.  `as`（`ld -o lzy lzy.o`）- 汇编器， 将汇编代码转换为目标文件。
 3. `objdump`(`objdump -d lzy`) -  用于显示二进制文件的详细信息，包括汇编代码、符号表等。
 4. `nm`(`nm lzy`)- 列出符号， 用于列出二进制文件中的符号。
 5. `objcopy`(`objcopy -o binary lzy.o lzy.bin`)- 它可以将目标文件转换为不同的格式，或者修改文件的内容。
![输入图片说明](/imgs/2025-03-09/tMgaSP0Y2F9XBxuT.png)
### 5. `Shell`脚本
### ① 思考：`shell` 为何知道用 `sh` 来解析这个文件?
 1. 通过 Shebang (`#!`) 指定`sh`解释器来解析这个文件。
 2. 通过 `.sh` 后缀知道它是`shell`文件在交互式`shell`中运行。

### ② 编写一个 `Shell` 脚本，统计一个文件中某个关键词出现的次数。
~~~bash
#!/bin/bash
read -p "请输入要搜索的文件路径: " filename
if [[ ! -f "$filename" ]]; then
    echo "错误: 文件不存在!"
    exit 1
fi
read -p "请输入要搜索的关键词: " keyword
count=$(grep -o "$keyword" "$filename" | wc -l)
echo "关键词 '$keyword' 在文件 '$filename' 中出现了 $count 次。"
~~~
![输入图片说明](/imgs/2025-03-09/wh2bRi3xdvjhGRpg.png)
### 6. 分析程序
### ①从这个程序的逻辑来看，不变量是什么？
#### 从`echo "$a + $b = $(($a+$b))"`来看系统不变量为：$a + $b=200
### ②从这个程序的执行来看，结果是否正确？
#### 不正确，如图
![输入图片说明](/imgs/2025-03-13/BHPen3xDk4Bu1Xsf.png)

### ③为什么？
####  因为并发操作下对共享文件的非原子读写导致了竞态条件。具体表现为：多个 `transfer` 进程同时执行时，会并发读取 `A.txt` 和 `B.txt` 的最后一行数值（如均读取到初始值 `100`），并基于旧值进行计算和写入。由于文件读写操作（`tail` 读取和 `echo` 追加）未加锁或同步，进程间可能交替**读取旧值和覆盖写入，导致答案错误。**。
## 实验总结
#### 本次实验通过实践掌握Linux文件管理（`ls/find/grep`）、权限控制（`chmod`特殊权限）及内存监控（`top/free`）等核心操作，重点学习Shell脚本编写与网络工具（`netstat/curl`）应用，并发场景下未加锁的转账脚本因竞态条件导致逻辑混乱，虽验证了不变量（A+B总和恒定），但暴露线程安全问题，同时通过命令与系统调用（如`touch`关联`open`）的关联深化底层理解，为系统部署和自动化运维奠定基础，并强化了规范操作意识。
  
   

<!--stackedit_data:
eyJoaXN0b3J5IjpbLTI2OTgwNjE0Myw3ODk4Mzg4MjgsMjE0MT
UyMDU0OCwtNzkzOTY5NjgsNDQxMTEyMDM4LDQ4NzI5NTk1Niwt
MTU0OTYzMjI5NCwtMTYxNTUyOTIyOSwtODc4MjU5NzcsODg3ND
Y4NjI4XX0=
-->